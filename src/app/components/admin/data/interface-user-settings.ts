export interface UserSettings {
  name: string;
  quantity: number;
  price: number;
  sku: string;
  image: string;
  available: boolean;
  token: string;
  description: string;
  category_id: string;
}